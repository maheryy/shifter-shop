.cache-client:
  cache:
    key:
      files:
        - client/package-lock.json
    paths:
      - client/.npm
    policy: pull-push
  artifacts:
    expire_in: 1h
    paths:
      - client/node_modules

build-client:
  stage: build
  image: node:18
  script:
    - cd client
    - npm ci --cache .npm --prefer-offline
    - npm run build
  extends: .cache-client
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - client/**/*

test-client:
  stage: test
  image: node:18
  script:
    - cd client
    - npm run test
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - client/**/*

deploy-client:
  stage: deploy
  image: node:18
  script:
    - cd client
    - echo "Deploying client"
  rules:
    - if: $CI_COMMIT_BRANCH == "dev" || $CI_COMMIT_BRANCH == "main"
      changes:
        - client/**/*
